eureka:
  client:
    service-url:
      #向两个注册中心注册
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: clientName #自定义点入的链接
server:
  port: 8081
spring:
  application:
    name: client